jobs:
 - reduce_sectors:
    reduce:
     - {sectors: [b0110], r: 4, s: 2} 
    select_integrals:
     select_mandatory_recursively:
      - {sectors: [b0110], r: 4, s: 2} 
    run_initiate: true
    run_triangular: true
    run_back_substitution: true