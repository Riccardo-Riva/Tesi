jobs:
 - reduce_sectors:
    reduce:
     - {sectors: [15], r: 6, s: 4}
    select_integrals:
     select_mandatory_recursively:
      - {sectors: [15], r: 6, s: 4}
    preferred_masters: my_basis
    run_initiate: true
    run_triangular: true
    run_back_substitution: true
