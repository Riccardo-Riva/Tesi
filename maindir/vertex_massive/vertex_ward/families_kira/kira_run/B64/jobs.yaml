jobs:
 - reduce_sectors:
    reduce:
     - {sectors: [b1111110], r: 6, s: 1} 
    select_integrals:
     select_mandatory_recursively:
      - {sectors: [b1111110], r: 6, s: 1} 
    integral_ordering: 2
    run_initiate: true
    run_triangular: true
    run_back_substitution: true